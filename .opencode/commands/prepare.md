---
description: Create issue and branch from requirements
---
Load and use the `prepare` skill.

Input (requirements doc path or task slug):
$ARGUMENTS

If a task slug is provided, resolve it to `workflow/requirements/{task-name}.md`.
Follow the skill workflow and output contract exactly.
